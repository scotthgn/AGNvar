Main source directory for the agn variability code. 
All the relevant models and methods lie in the agnvar.py file,
So this is really the only one you need to worry about
(Although you will still need to download pyNTHCOMP, as this gets
Called by some of the models...)
